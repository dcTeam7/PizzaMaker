// import mergeImages from 'merge-images';
// https://www.npmjs.com/package/merge-images

export default async () => {

    // usar o mergeImages para mesclar as tres imagens
    // imgs/borda-01.png, imgs/molho-vermelho.png imgs/recheio-01.png
    
    // receber a base64
    // transformar a base64 em arquivo 
    // salvar o arquivo no diretorio server/output.png

    return // path para o output.png
}